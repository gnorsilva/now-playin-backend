# Test conf

now-playin {
  loglevel = INFO

  mongo {
    uri = "mongodb://localhost/now-playin-test"
  }

  twitter {
    api {
      url = "http://localhost:8080"
    }
    oauth {
      consumer-key = "somerandomkey"
      consumer-secret = "somerandomsecret"
      token = "somerandomtoken"
      token-secret = "somerandomtokensecret"
    }
  }
}

